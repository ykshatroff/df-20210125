version: '2.1'

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile-dev
    ports:
      - "8000:80"
    volumes:
      - ".:/app"
#    command: gunicorn main:app --workers 2 --bind :80
    command: uvicorn main:app --reload --host 0.0.0.0 --port 80
    environment:
      SECRET: "${SECRET}"
      PYTHONPATH: "/app/df"
